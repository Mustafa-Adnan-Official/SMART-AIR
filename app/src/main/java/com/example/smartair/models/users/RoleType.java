package com.example.smartair.models.users;

/**
 * Purpose: Defines the three user roles in the app.
 * Used For: Login routing and access logic.
 */
public enum RoleType {
    PARENT,
    PROVIDER,
    CHILD
}