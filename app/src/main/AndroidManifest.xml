<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartAir">

        <!-- Entry point -->
        <activity
            android:name=".ui.HomeActivities.WelcomeRoleSelectionActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Signup screens -->
        <activity android:name=".ui.signup.ParentSignupActivity" />
        <activity android:name=".ui.signup.ChildSignupActivity" />
        <activity android:name=".ui.signup.ProviderSignupActivity" />

        <!-- Login screens -->
        <activity android:name=".ui.login.ParentLoginActivity" />
        <activity android:name=".ui.login.ChildLoginActivity" />
        <activity android:name=".ui.login.ProviderLoginActivity" />

        <!-- Home screens -->
        <activity
            android:name=".ui.HomeActivities.ParentHomeActivity"
            android:exported="true" />

        <activity android:name=".ui.HomeActivities.ChildHomeActivity" />
        <activity android:name=".ui.HomeActivities.ProviderHomeActivity" />

        <!-- Settings / Management -->
        <activity android:name=".ui.ActivitySettingsChild" />
        <activity android:name=".ui.ActivitySettingsProvider" />

        <!-- Onboarding -->
        <activity android:name=".ui.onboarding.ChildOnboardingActivity" />
        <activity android:name=".ui.onboarding.ParentOnboardingActivity" />
        <activity android:name=".ui.onboarding.ProviderOnboardingActivity" />
        
        <activity android:name=".ui.checkin.DailyCheckinActivity" />
        <activity android:name=".ui.checkin.DailyCheckinActivityParent" />
        
        <activity android:name=".ui.historyBrowser.HistoryBrowserActivity" />

        <!-- R3: Medicine logging -->
        <activity android:name=".ui.meds.RescueSessionActivity"
            android:exported="false" />

        <activity android:name=".ui.meds.ControllerSessionActivity"
            android:exported="false" />

        <!-- R3: Technique trainer -->
        <activity android:name=".ui.technique.TechniqueTrainerActivity"
            android:exported="false" />

        <!-- R3: Achievements -->
        <activity android:name=".ui.motivation.ChildAchievementsActivity"
            android:exported="false" />
    </application>

</manifest>